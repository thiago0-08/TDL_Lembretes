import 'package:flutter/material.dart';

class SeusLembretesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Seus Lembretes")),
      body: Center(
        child: Text("Lista de Lembretes"),
      ),
    );
  }
}
