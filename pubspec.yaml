name: tdl_lembretes
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.7.2 <4.0.0" 

dependencies:
  flutter:
    sdk: flutter
  curved_navigation_bar: ^1.0.6 # Barra de navegação
  phosphor_flutter: ^2.0.0 # Icones
  cupertino_icons: ^1.0.8 # Icones 
  board_datetime_picker: 2.5.0 # calendario

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  google_fonts: ^6.1.0 

flutter:
  uses-material-design: true
  assets:
    - assets/tdl.png
    - assets/testeimg.png
    - assets/giftcard.jpg
    - assets/giftcard.jpeg
